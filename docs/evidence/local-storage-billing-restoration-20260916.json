{
  "recordedAt": "2026-09-15T22:28:50Z",
  "scope": "durable storage billing restoration, generation fencing, and Node provider binding",
  "sourceCommit": "f4eea80d4d9ffa228c9e450fa53ae5942cfb383c",
  "checkout": "/private/tmp/private-skills-final-candidate-4917215",
  "environment": "Local-only validation. Billing and database checks used disposable loopback PostgreSQL on 127.0.0.1:55432; credentials and connection values are omitted. No production database, storage provider, deployment, or external identity provider was contacted.",
  "checks": [
    {
      "command": "CI=true pnpm typecheck",
      "result": "passed"
    },
    {
      "command": "CI=true node_modules/.bin/vitest run packages/billing/test/index.test.ts packages/billing/test/postgres.integration.test.ts packages/storage/test/recovery.test.ts packages/storage/test/recovery.postgres.integration.test.ts packages/storage/test/node.test.ts packages/storage/test/receipt.test.ts packages/core/test/storage-receipt.test.ts packages/core/test/storage-recovery.test.ts packages/core/test/index.test.ts packages/authoring/test/drafts.test.ts apps/web/src/runtime-node.test.ts --no-file-parallelism",
      "result": "11 test files passed; 134 tests passed (focused billing, recovery, receipt, authoring, core, Node binding, and loopback PostgreSQL coverage)"
    },
    {
      "command": "CI=true node_modules/.bin/vitest run packages/storage/test/recovery.postgres.integration.test.ts --no-file-parallelism",
      "result": "1 test file passed; 2 tests passed directly against disposable loopback PostgreSQL, including B-before-zero fencing, B-after-zero restoration, late G1 rejection, and lost resolver response replay"
    },
    {
      "command": "git diff --check",
      "result": "passed"
    }
  ],
  "coveredAreas": [
    "ledger-owned restoreUsage compensation bypasses ordinary quota admission while preserving future cap enforcement",
    "restoration records persist source reservation key, source generation, target generation, and exact storage delta",
    "lost restoration responses replay through the durable exact operation key without a second usage write",
    "the PostgreSQL-backed StorageRecoveryService resolver fences an untouched G1, restores a committed G1 zero, and rejects delayed G1 cleanup",
    "stale generation-zero callbacks are rejected after G2 restoration and after later G3 re-admission",
    "PostgreSQL exact-key lookup survives bounded operation-window eviction and a fresh BillingService instance",
    "storage recovery persists G2 before clearing restore-pending and remains fail-closed without restoreUsage",
    "the PostgreSQL-backed StorageRecoveryService bridge exercises quota refill, late metadata reference, lost restore response, restart replay, stale G1 zero, and G2 cleanup",
    "Node runtime configuration carries the non-secret provider binding and Blob store ID; a filesystem adapter restart proves receipt-bound cleanup while an unknown write remains retained"
  ],
  "limits": [
    "provider finality remains false for the built-in hosted adapter unless the host supplies a terminal write proof",
    "hosted storage recovery and production deployment acceptance remain open",
    "provider finality for hosted adapters, runtime-host integration, and independent worker/receipt follow-ups remain outside this local proof",
    "this evidence record does not establish release readiness"
  ]
}
