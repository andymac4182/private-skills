# Company SSO SAML fixture

The certificate and private key in this directory are self-signed, disposable
test material used only to exercise the Better Auth signed-assertion path. They
do not identify a real company or IdP and must never be used outside tests.
